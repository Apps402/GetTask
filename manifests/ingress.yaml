apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gettask-ingress
spec:
  ingressClassName: azure-application-gateway
  rules:
    - host: gettask.aparnasapps.icu
      http:
        paths:
        -  pathType: Prefix
           path: "/"
           backend:
             service:
               name: gettask-svc
               port:
                 number: 80
